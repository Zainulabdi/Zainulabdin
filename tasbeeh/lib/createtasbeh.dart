import 'package:flutter/material.dart';
import 'mainpage.dart';

class createtasbeh extends StatelessWidget {
  const createtasbeh({key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Create Tasbeeh'),
      ),
      body: Center(

      ),
    );
  }
}

